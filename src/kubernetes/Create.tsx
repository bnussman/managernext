import { Card, CardBody, Heading } from "@chakra-ui/react";

export function Create() {
  return (
    <form>
      <Heading size="lg" letterSpacing="tight" mb={4}>Create Cluster</Heading>
      <Card variant="outline">
        <CardBody>
        </CardBody>
      </Card>
    </form>
  );
}